# Git Push Test

This repository contains a small HTML/CSS example used to verify git push connectivity.

Files:
- `index.html` — simple test page
- `style.css` — styling for the page
- `README.md` — this file

To test your GitHub push:
1. `git add .`
2. `git commit -m "Add push test files"`
3. `git push`
